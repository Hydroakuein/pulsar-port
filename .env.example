# Public URL used by the Vue app. VITE_ values are bundled into the client.
VITE_API_BASE_URL=http://127.0.0.1:8787

# Used only by Wrangler when deploying. Never prefix server secrets with VITE_.
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=
